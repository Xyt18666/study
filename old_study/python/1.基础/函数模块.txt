定义函数：

def myFn():
	内容

-------------------------------

函数的调用：

myFn()

---------------------------

函数的传参：

def myFn(name):
	print(name + 内容)

-------------------------------

函数 的 返回值 ：
	
def myFn(name):
	return (name + 2)

------------------------------------
形参
实参

----------------------------------

函数文档：
def myFn(name):
	'编写函数文档'
	内容

获取函数文档内容：  fn.__doc__

-------------------------------------
关键字参数：
def fn(name,words):
	print(name,words)

fn(name=22,words=11)
-----------------------------------
默认值参数：
def fn(name="333",words="444"):
	print(name,words)

fn()

------------------------------------
收集参数：
def fn(*parans):
	print(parans[0],parans[1],parans[2])

fn(7,8,9)

-----------------------------------------
函数的返回值：

当把某个 函数 赋值到 变量时 ，
如果没有 return ,变量拿到的将是一个 none

-----------------------------------------------
函数变量的作用域：

在函数内定义的参数和变量 ，都是局部变量
在函数内可以使用全局变量

-----------------------------------------------

在函数内修改 全部变量：
xxx = 5
def fn():
	golbal xxx
	xxx = 10
fn()

此时全局被修改 ， 使用了 关键字 glibal

-----------------------------------------------















